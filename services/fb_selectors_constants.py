
# Selectors for Facebook Business Suite (Example headers)
# These often change, so robust XPATH or Text search is better.

BTN_CREATE_POST = "//div[text()='Create post' or text()='Tạo bài viết' or contains(@aria-label, 'Create post')]"
BTN_ADD_VIDEO = "//div[contains(text(), 'Add video') or contains(text(), 'Thêm video')]"
INPUT_FILE = "//input[@type='file']"
# ...
